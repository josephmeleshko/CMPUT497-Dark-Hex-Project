Dark Hex Server

python3 DarkHexMain.py

Follow instructions to configure game.
Mohex agent requires mohex executable installed somewhere on system, location can be specified in setup.

Manual mode can be enabled by editing DarkHexMain.py and manually configuring the settings for the Dark Hex Server instead of following the setup procedure that is the result of just running DarkHexMain.py
